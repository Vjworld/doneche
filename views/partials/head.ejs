<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>doneche — Stop wondering if you got ghosted</title>
<meta name="description" content="Track your job applications, get auto-flagged the moment a company goes silent for 7+ days, and send a one-click follow-up email. Built for frustrated job seekers." />

<!-- Open Graph / Twitter Card (social share previews) -->
<meta property="og:type" content="website" />
<meta property="og:title" content="doneche — Stop wondering if you got ghosted" />
<meta property="og:description" content="Track your job applications, get auto-flagged the moment a company goes silent for 7+ days, and send a one-click follow-up email." />
<meta property="og:url" content="https://doneche.shabdly.online/" />

<meta property="og:site_name" content="doneche" />
<meta name="twitter:card" content="summary" />
<meta name="twitter:title" content="doneche — Stop wondering if you got ghosted" />
<meta name="twitter:description" content="Track your job applications, get auto-flagged the moment a company goes silent for 7+ days, and send a one-click follow-up email." />

<!-- GEO/SEO: structured data for AI search + rich results -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "doneche",
  "description": "Job application tracker that auto-flags companies that go silent (ghosted) for 7+ days and generates one-click follow-up emails.",
  "applicationCategory": "BusinessApplication",
  "operatingSystem": "Web",
  "offers": [
    { "@type": "Offer", "name": "Free", "price": "0", "priceCurrency": "INR" },
    { "@type": "Offer", "name": "Pro", "price": "399", "priceCurrency": "INR" }
  ]
}
</script>


<!--
  Single Google AdSense verification point.
  Only ONE verification method is used for this project (meta tag, below).
  Do NOT also add the ads.txt method or a second script-based verification —
  that would trigger duplicate-site-ownership conflicts in AdSense.
  Set ADSENSE_CLIENT_ID (e.g. ca-pub-XXXXXXXXXXXXXXXX) as an env var to activate.
-->
<% if (typeof adsenseClientId !== 'undefined' && adsenseClientId) { %>
<meta name="google-adsense-account" content="<%= adsenseClientId %>" />
<% } %>

<link rel="stylesheet" href="/style.css" />

<!-- Apply saved theme preference ASAP to avoid a flash of the wrong theme.
     Priority: logged-in user's saved theme_preference (passed from server) > localStorage > light default. -->
<script>
  (function () {
    var serverTheme = <%- (typeof user !== 'undefined' && user && user.theme_preference) ? JSON.stringify(user.theme_preference) : 'null' %>;
    var theme = serverTheme || localStorage.getItem('doneche_theme') || 'light';
    document.documentElement.setAttribute('data-theme', theme);
    localStorage.setItem('doneche_theme', theme);
  })();
</script>


<!-- driver.js — lightweight onboarding tour library -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/driver.js@1/dist/driver.css" />
<script src="https://cdn.jsdelivr.net/npm/driver.js@1/dist/driver.js.iife.js"></script>

<!-- Shared UI helpers: toast notifications, button/page loaders, global error safety-net -->
<script src="/ui.js"></script>



